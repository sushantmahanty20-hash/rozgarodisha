import { RecruiterSettingsPage } from "@/components/dashboard/recruiter-settings";

export const metadata = { title: "Settings" };

export default function SettingsPageWrapper() {
  return <RecruiterSettingsPage />;
}