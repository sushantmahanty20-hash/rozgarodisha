import { CandidatesPage } from "@/components/dashboard/candidates";

export const metadata = { title: "Candidate Database" };

export default function CandidatesPageWrapper() {
  return <CandidatesPage />;
}